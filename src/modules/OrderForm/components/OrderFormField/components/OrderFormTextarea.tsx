import { OtherFieldsConfig } from "../fieldConfigTypes"

export const OrderFormTextarea = ({
  name,
  label,
  className,
  fieldType,
  placeholder
} : OtherFieldsConfig) => {

  return (
    <></>
  )
}