import { OtherFieldsConfig } from "../fieldConfigTypes";

export const OrderFormImput = ({
  name,
  label,
  className,
  fieldType,
  placeholder
} : OtherFieldsConfig) => {

  return (
    <></>
  )
}