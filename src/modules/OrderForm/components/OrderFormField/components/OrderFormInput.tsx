import { OtherFieldsConfig } from "../fieldConfigTypes";

export const OrderFormInput = ({
  name,
  label,
  className,
  fieldType,
  placeholder
} : OtherFieldsConfig) => {

  return (
    <></>
  )
}