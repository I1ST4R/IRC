import { OtherFieldsConfig } from "../fieldConfigTypes";

export const OrderFormDate = ({
  name,
  label,
  className,
  fieldType,
  placeholder
} : OtherFieldsConfig) => {

  console.log()

  return (
    <></>
  )
}